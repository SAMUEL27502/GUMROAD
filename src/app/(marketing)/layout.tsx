/**
 * Marketing route group — public pages (home, marketplace, blog, auth entry, legal).
 * URLs are unchanged; parentheses do not appear in the path.
 */
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
