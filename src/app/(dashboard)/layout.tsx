/**
 * Dashboard route group — authenticated product surfaces
 * (dashboard, profile, MT5, billing, notifications, referrals).
 */
export default function DashboardGroupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
